## Task Statement 1.1 Lesson 2

> Know how/where which services to use for which data variety, un/semi/structured

- Structured
    - RDS
    - Aurora
    - Redshift
    - S3
    - Athena

- Semi-structured
    - DynamoDB
    - DocumentDB
    - Athena 
    - S3

- Unstructured
    - S3
    - Rekognition
    - Transcribe
    - Comprehend        

Notice S3 is in each.

> may also need to understand/consider compression or splitable formats


### Programming concepts in AWS
- `Ingestion`: Lambda
- `Processing`: EMR, Glue, Spark, Kinesis
- `Transformations`: Glue
- `Storage`: S3, RDS, Redshift
- `Orchestration`: Step Functions (Lambda generator)


_TODO_ Need to know the ins and outs of Glue. 

> For the Exam, make sure you know

- Kinesis
    - Kinesis Data streams
    - Kinesis Video Stream
- Firehose
- Managed Service for Apache Flink
- Managed streaming for Kafka (MSK)
- Amazon AppFlow

#### Question

> You are using Amazon Firehose delivery stream to ingest GZIP compressed data records from on on-prem application.  You need to configure a solution for your data scientist to _perform SQL queries the data stream_ for real-time insights

> What is your solution


`perform SQL queries the data stream`

> One solution is Managed Service for Apache Flink, using Lambda.

> Another solution is to store data in S3 buckets and use Athena queries. 'Realtime' is part of the requirements and Athena does not offer real-time insights

> Another solution, `AWS DMS` (not seen this before), to convert/compress (I guess), store into S3, use Glue Data Catalog, Athena.  Then use SageMaker to do stuff??.. This solution is best because compression.  `Faster pipe mode` transfers data faster.


#### Question
> You are a machine learning engineer and you need to process a large amount of customer data, analyze the data, get insights so the analysts can make further decisions.  To accomplish this task, you need to store the data in a data structure that can handle large volumes of data efficiently retrieve it as fast as possible 

> What is the solution?

> One solution, use EMR to store data and retrieve fast, Hadoop Dist File System (HDFS) cluster, process data, MapReduce algorithm.

For this task statement you also need to understand __Amazon SageMaker Canvas__
- Data Flow
- Transform
- Generate Data Insights
- Analyze
- Export
    - S3
    - SM Feature store
- Automate data preparation
    - SageMaker Model Builder Piplines
    - Serial inference pipeline
    - Python script

So I guess the goal is to get data into the feature store

> Is there another way to bring data into the data store, yes! Spark and connector and something... I think the point is there is more than one way.

